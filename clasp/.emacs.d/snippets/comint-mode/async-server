# -*- mode: snippet -*-
# name: async-server
# key: <async-server
# --
-module(${1:module_name}).
-export([loop/0]).

loop() ->
    receive 
        {${2:patter_name}} ->
            ${3:tasks}
            loop();
        
    end.
