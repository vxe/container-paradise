# -*- mode: snippet -*-
# name: clush-nic-speed
# key: clushnicspeed
# --
export SITE=$1 ;clush -s $SITE -g api -g apift -g cimage -g csearch -g cshared -g cstring -g cstate -g crelay -g lyraa -g lyrab -g ds -B "sudo ethtool \\\$(sudo facter -p default_interface) | grep -i speed"